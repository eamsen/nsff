// #ifndef G_H
// #define G_H
#include <iostream>
#include <string>
#include <vector>
#include <set>
#include <sstream>
#include <algorithm>
// #include <cassert>
#include <queue>
using namespace std;
#define ty typedef
#define gl getline
#define w(p) while(p)
#define f(p) for(p)
#define b() back()
#define po pop
#define pb(p) push_back(p)
#define r return
#define fi first
#define se second
ty int i;
ty uint32_t u;
ty string s;
ty vector<u> vu;
ty vector<s> vs;
struct S;
ty vector<S> vS;
ty priority_queue<S> qS;
struct A;
ty vector<A> vA;
ty stringstream ss;
ty pair<u,u> uu;
ty pair<u,pair<u,u> > uuu;
ty vector<uu> vuu;
ty vector<uuu> vuuu;
ty set<uuu> suuu;
ostream& co=cout;
istream& ci=cin;
#include "s.h"
// #endif  // G_H
